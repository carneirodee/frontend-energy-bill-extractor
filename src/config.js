const BACKEND_ENDPOINT = "http://localhost:8080/";
const BACKEND_ENDPOINT_PRODUCTION = "https://backend-energy-bill-extractor.onrender.com/";

const NODE_ENV = "production";

 export  {
    BACKEND_ENDPOINT,
    BACKEND_ENDPOINT_PRODUCTION,
    NODE_ENV
}